"""Diffusion-pipe backend: wraps tdrussell/diffusion-pipe as the training engine.

v0.2 ships only the install + selection scaffold. The recipe-to-argv compiler
and a real `launch()` arrive in v0.3.
"""
