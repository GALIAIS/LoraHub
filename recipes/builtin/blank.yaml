# Built-in recipe template: minimal skeleton, fill every field manually.
# REQUIRED before saving: base_model.checkpoint, dataset.source.
# Everything else falls back to RecipeConfig defaults.
# The _template block is metadata for GET /api/recipes/templates and is
# stripped before the document is validated against RecipeConfig.

_template:
  name: Blank
  description: Minimal skeleton — fill every field manually.
  arch: sdxl

base_model:
  arch: sdxl
  checkpoint: ""

dataset:
  source: ""
