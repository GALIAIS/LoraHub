[package]
name = "lorahub-desktop"
version = "0.1.0"
description = "LoraHub desktop shell"
authors = ["LoraHub"]
edition = "2021"

[lib]
name = "lorahub_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
